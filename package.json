{
  "scripts": {
    "test": "testcafe chrome test/specs/*.js -r xunit:report.xml,spec -S -s tmp/artifacts"
  },
  "devDependencies": {
    "testcafe": "^0.18.4",
    "testcafe-reporter-xunit": "^2.1.0"
  }
}
